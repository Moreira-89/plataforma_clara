# Dependências da Plataforma Clara — arquivo único.
#
# Python 3.12: o pandas~=2.3.3 não tem wheel para 3.14, e a instalação falha ao
# compilar dependências transitivas.
#
# Só entra aqui o que é importado de verdade. Bibliotecas de fases futuras entram
# na fase que as usa, não antes — `langgraph` ficou um roadmap inteiro declarado
# e nunca importado.

# --- Entrega: UI atual (Reflex) e API (FastAPI, Fase 2) ---------------------
# Os dois coexistem enquanto as rotas migram uma a uma. Verificado: instalam
# juntos sem conflito (o Reflex traz o Starlette, não o FastAPI).
reflex~=0.8.28.post1
fastapi~=0.141.1
uvicorn[standard]~=0.52.4
pydantic-settings~=2.15.0
PyJWT~=2.13.0
python-multipart~=0.0.32   # exigido pelo FastAPI para upload de CSV e form de login

# --- Domínio e persistência -------------------------------------------------
# sqlmodel, sqlalchemy e pydantic chegavam de carona pelo Reflex, mas são
# importados direto em domain/ e infra/ — declarados para que a saída do Reflex
# na Fase 5 não leve a camada de dados junto.
pydantic~=2.13.5
sqlmodel~=0.0.42
SQLAlchemy~=2.0.52
alembic~=1.19.2
psycopg2-binary~=2.9.11
python-dotenv~=1.2.2
bcrypt~=5.0.0
pandas~=2.3.3

# --- Analytics e relatório por IA -------------------------------------------
google-cloud-bigquery~=3.40.1
google-auth~=2.57.1
db-dtypes~=1.5.1           # sem ele, .to_dataframe() falha em colunas DATE/NUMERIC
groq~=0.37.1
langchain-core~=1.6.2
langchain-groq~=1.1.2
markdown-pdf~=1.13.1
PyPDF2~=3.0.1              # projeto descontinuado; sucessor é `pypdf`

# --- Desenvolvimento e CI ---------------------------------------------------
# Sem arquivo separado: a imagem Docker instala este mesmo requirements e passa
# a carregar as ferramentas de teste junto.
pytest~=8.4.2
pytest-cov~=6.3.0
ruff~=0.16.6
httpx~=0.28.1              # cliente do TestClient, para os testes da API da Fase 2
