# Processado pelo docker-entrypoint.sh: ${PORT} só é conhecida em runtime.
server {
    listen ${PORT};
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA: rota desconhecida devolve o index.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
